# AGENTS.md — CV. ADI WIJAYA LANGGENG (Landing Page)

## Info Dasar
- Landing page satu halaman (single page, tanpa framework) untuk distributor packaging **CV. ADI WIJAYA LANGGENG** (pering usha: AWOL Packaging).
- Perusahaan menaungi **4 outlet**:
  1. AWOL Packaging — Yogyakarta
  2. AWOL Packaging — Sleman
  3. ADI WIJAYA Packaging — Klaten
  4. ASIA LESTARI Packaging — Madiun
- Lokasi: `C:\Users\Lenovo\Documents\awol-packaging\`
- File utama: `index.html` (dibuka langsung via browser).
- **Hosting (sejak 2026-09-25): GitHub Pages.** Live: `https://awpackagingklaten-prog.github.io/awol-packaging/` (repo publik: `https://github.com/awpackagingklaten-prog/awol-packaging`). **Update situs = commit + push ke branch `main`** (Pages auto-build). Token PAT disimpan di Windows Credential Manager; GitHub CLI (`gh`) di `C:\Program Files\GitHub CLI`.
- Halaman katalog: `produk.html` — **"Kategori Produk"** (ditambah lewat tombol "Lihat Semua Produk" di bagian bawah section Produk yang membuka **tab baru** (`target="_blank"`); anchor `#kategori-produk`). Berisi 44 kartu varian (4 kardus, 6 bubble, 3 foam, 11 lakban, 12 plastik, 2 tali, 6 aksesoris) yang di-render data-driven dari `assets/js/products.js` (lihat Panduan Edit Data di bawah). **Since 2026-09-25 KERANJANG ada di `index.html` DAN `produk.html`** (checkout via WhatsApp, data pemesan + pilih outlet wajib — lihat Logika Penting).
- Halaman **Kebijakan Privasi & Syarat**: `privacy.html` (link "Kebijakan Privasi & Syarat" di bar bawah footer `index.html`; ikut dikompilasi di Tailwind config).

## Struktur Landing Page
1. Navbar (brand CV. ADI WIJAYA LANGGENG)
2. Hero — nama CV + badge "4 Outlet" + **CTA ganda: "Chat WhatsApp" + "Lihat Kategori Produk"** (CTA WA di hero dihidupkan kembali sejak dihapus 09-18, sebagai konversi utama untuk iklan Google Ads)
3. Produk Unggulan — 3 kartu slider (Kardus, Lakban, Bubble Wrap), tiap kartu punya tombol **"Tambah ke Keranjang"** (`data-add`, sejak 2026-09-26) + tombol "Lihat Semua Produk" → `produk.html` dibuka **di tab baru** (katalog lengkap 7 kategori)
4. **Outlet** — 4 kartu outlet (nama, kota, tombol Google Maps)
5. Keunggulan — 4 poin (termasuk "4 Outlet Strategis")
6. Tentang — CV induk + daftar 4 outlet
7. Cara Pesan — 4 langkah (dari outlet terdekat). Keterangan pembayaran **tidak** dibuat section/kartu terpisah — cukup di dalam **poin 3 "Konfirmasi & Bayar"**: "…Tidak wajib bayar di muka — bisa dibayar di tempat ketika barang sudah sampai di alamat Anda atau saat mengambil langsung di outlet." (Permintaan pengguna 2026-09-26: jangan tambah section baru.)
8. Testimoni
9. Kontak — **cuma blok CTA oranye sejak 2026-09-26**: `<section id="kontak" class="py-20 sm:py-28 hero-gradient">` dengan satu judul "Lokasi Anda Jauh dari Outlet - Outlet Kami? Namun Butuh Packaging dalam Jumlah Besar? Semua Solusi Ada di Kami..!!" + tombol **"Chat WhatsApp Sekarang"** (WA utama). Tanpa form, tanpa badge, tanpa dropdown. **Riwayat 2026-09-26**: form kontak (5 field) → dihapus, sempat diganti blok putih "Butuh Packaging dalam Jumlah Besar?" + tombol "Hubungi Outlet" (dropdown 4 outlet) → lalu dipangkas lagi menjadi blok oranye di atas. **Jangan tambah isi baru di `#kontak` tanpa diminta.**
10. Footer
11. ~~Tombol WhatsApp melayang~~ — **DIHAPUS 2026-09-26** (bersama popup pilih outlet `#float-wa-wrap`); posisinya sekarang dipakai FAB keranjang

**Section CTA Banner (yang tadinya di bawah Kontak) SUDAH DIHAPUS 2026-09-26** karena isinya duplikat dengan blok Kontak — urutan section sekarang: Navbar → Hero → Produk Unggulan → Outlet → Keunggulan → Tentang → Cara Pesan → Testimoni → Kontak (oranye) → Footer → tombol keranjang mengambang (kanan bawah).

Halaman `produk.html` (katalog) tambahan: tombol **Keranjang** di navbar + tombol keranjang mengambang (kanan bawah) → panel geser berisi daftar item, data pemesan, pilihan outlet, dan tombol kirim pesanan. **Halaman `index.html` punya keranjang yang sama persis** (FAB **kanan bawah**, posisi bekas tombol WA melayang; tombol WA melayang, logo keranjang navbar, dan teks "Keranjang" di menu mobile sudah dihapus 2026-09-26 — akses keranjang beranda hanya lewat FAB, sama untuk desktop & HP).

## Logika Penting
- Warna utama: **Oranye** (brand: #f97316, #ea580c; definisi di `tailwind.config.js` proyek). **Tailwind TIDAK lagi via CDN** — sejak 2026-09-25 CSS dikompilasi statis (Tailwind standalone CLI v3.4.17): source `assets/css/input.css` + config `tailwind.config.js` → output `assets/css/style.css`, lalu `<link rel="stylesheet" href="assets/css/style.css">` di `<head>` index & produk. **Saat menambah/mengganti kelas Tailwind wajib rebuild**: `tailwindcss.exe -i assets/css/input.css -o assets/css/style.css --config tailwind.config.js --minify` (binary di `C:\Users\Lenovo\AppData\Local\Temp\opencode\tailwindcss.exe`).
- **Tracking Google Analytics / Google Ads (gtag.js)**: snippet + event konversi terpasang di `index.html` & `produk.html` — klik `wa.me` = event **`generate_lead`** (konversi utama, otomatis via delegasi klik), klik `shopee.co.id` = event terpisah **`outbound_click`** (tidak mencemari konversi). Tambah item ke keranjang = event **`add_to_cart`** (pakai `currency: 'IDR'` + `items[]`). **Measurement ID GA4 asli sudah terpasang: `G-N42Q7Z749M`** (2 baris per file: `src=...gtag/js?id=` & `gtag('config', ...)`). Untuk konversi Google Ads (AW-...) tinggal tambah `send_to` di blok `generate_lead`. **Catatan 2026-09-26**: event `generate_lead` kategori `lead_form` (submit form kontak) DIHAPUS bersama form kontak.
- **Nomor WhatsApp:**
  - Utama (navbar, hero, tombol "Chat WhatsApp Sekarang" di Kontak, email footer): `628211395050` — asli.
  - Yogyakarta: `6285156338939` — asli.
  - Sleman: `62895400927385` — asli.
  - Klaten: `628998000502` — asli.
  - Madiun: `6281220408070` — asli.
  - Nomor per outlet ditulis langsung di `href` (`wa.me/...`) tiap markup: kartu outlet, dropdown navbar, footer. Peta JS per outlet ada di `OUTLET_PILIHAN` (`assets/js/cart.js`) untuk keranjang.
- Link Google Maps outlet kini memakai link share persis: Yogyakarta `https://maps.app.goo.gl/yq8Tm8ZDXQrhKKDr9`, Sleman `https://maps.app.goo.gl/SsJg6kHtCicX13ce8`, Klaten `https://maps.app.goo.gl/M1fY5dvN6XWYj5pU7`, Madiun `https://maps.app.goo.gl/54sojNfcUcS8cDTz9` (12 lokasi: kartu outlet, kontak, footer).
- **Link toko Shopee per outlet:**
  - Yogyakarta: `https://shopee.co.id/awolpackagingjogja`
  - Sleman: `https://shopee.co.id/awolpackagingsleman`
  - Klaten: `https://shopee.co.id/wijaya.packaging`
  - Madiun: `https://shopee.co.id/lestari.packaging`
  - Dipasang di: kartu outlet (tombol "Toko Shopee" oranye `#ee4d2d`), kontak (teks "Shopee (username) →"), footer ("· Shopee").
- Alamat detail outlet sudah terisi (kartu outlet, kontak, dan JSON-LD):
  - Yogyakarta: Jl. Tunggul Nogo No.362A, Giwangan, Kec. Umbulharjo, Kota Yogyakarta 55163 (AWOL Packaging)
  - Sleman: Jl. Kaliurang Timur, Tambakan, Sinduharjo, Kec. Ngaglik, Kab. Sleman 55581 (AWOL Packaging)
  - Klaten: Jl. Dr. Cipto Mangun Kusumo, Jetak Kidul, Karanganom, Kec. Klaten Utara, Kab. Klaten 57438 (ADI WIJAYA Packaging)
  - Madiun: Jl. Cokrobasonto No.47, Josenan, Kec. Taman, Kota Madiun 63134 (ASIA LESTARI Packaging)
- **SEO**: meta description + keywords + favicon + Open Graph (`og:title/description/type/image/locale/url`) + canonical + **JSON-LD LocalBusiness** (1 `Organization` + 4 `Store`, dengan alamat, telepon, jam buka Senin–Sabtu 08:00–17:00, `sameAs` Shopee) sudah terpasang di `<head>` `index.html` & `produk.html`. Organization sejak 2026-09-25 juga memuat `logo`, `image`, dan `contactPoint` (sales, +628211395050, bahasa Indonesia, areaServed ID). **Domain placeholder terpasang: `https://awolpackaging.com/`** (og:url, canonical, dan `url`/`@id` Organization di JSON-LD; produk.html pakai `/produk.html`). Store masih memakai `@id` relatif fragment + `url` link share Google Maps. Saat domain benar-benar aktif & SSL tersedia, tinggal pastikan URL sudah benar.
- **Hero image `backgroud.png` kini responsif + WebP (sejak 2026-09-25)**: `<picture>` di hero `index.html` + `<link rel="preload">` di `<head>`. Browser modern mengunduh `backgroud.webp` (900px, **115 KB**) atau `backgroud-600.webp` (600px, **61 KB**) — hanya browser lawas jatuh ke PNG (`backgroud-600.png` 571 KB / `backgroud.png` 1.224 KB, backup asli tetap ada). Image hero pakai `decoding="async" fetchpriority="high"` (TIDAK lazy — itu LCP). Tool WebP: `C:\Users\Lenovo\AppData\Local\Temp\opencode\libwebp\...\bin\cwebp.exe`.
- Harga produk: **semua sudah harga asli yang dikonfirmasi pengguna (2026-09-25)** — termasuk foam (150.000/lembar-roll), plastik klip/pouch/ziplock/karung (15.000–25.000), kardus honeycomb (6.000/lembar), bubble roll utuh (90.000/roll).
- **Foto produk per kategori** (semua 384×384 px, **JPG** sejak 2026-09-24 terkompresi dari PNG, dinamai sesuai varian produk): Kardus 4 (`kardus-single-face`, `kardus-box`, `kardus-lembaran`, `kardus-honeycomb`), Bubble Wrap 6 (`bubble-roll`, `bubble-alumunium-foil`, `bubble-inflatable`, `bubble-meteran`, `bubble-potongan`, `bubble-mailer` — 2 foto baru `bubble-alumunium-foil` & `bubble-inflatable` ditambahkan pengguna 2026-09-25 & dikonversi PNG→JPG 384×384), Foam 3 (`foam-bounded`, `foam-bounded-2`, `foam-pe-sheet`), Lakban 11 (`lakban-OPP`, `lakban-fragile`, `lakban-double`, `lakban-double-tape-tebal-transparant`, `lakban-double-tape-foam`, `lakban-kertas`, `lakban-alumunium-foil`, `lakban-anti-bocor`, `lakban-clout-tape`, `lakban-police-line`, `isolasi-listrik`), Plastik 12 (`plastik-wrapping`, `plastik-polymailer`, `plastik-klip`, `plastik-pouch`, `plastik-ziplock`, `plastik-clingwrap`, `plastik-hdpe`, `plastik-trash-bag`, `plastik-karung`, `plastik-srink`, `plastik-vacum-embos`, `plastik-kresek`) — 4 foto baru (`plastik-klip`, `plastik-pouch`, `plastik-ziplock`, `plastik-karung`) ditambahkan pengguna 2026-09-25 dan sudah dikonversi **PNG → JPG** (384×384, kualitas 80, 19–25 KB; PNG asli di-backup ke temp `awol_images_backup`); Tali & Rakitan 2 (`tali-straping-band`, `tali-rafia`), Aksesoris Lainnya 6 (`aksesoris-thermal-resi`, `aksesoris-thermal-roll`, `aksesoris-hvs`, `aksesoris-masker`, `aksesoris-hair-net`, `aksesoris-sarung-tangan-latex`). Di `index.html` tampil sebagai slider full-bleed (`product-slider`, autoplay, **3 slider — hanya Produk Unggulan: Kardus, Lakban, Bubble Wrap**); di `produk.html` sebagai kartu katalog (total 44 kartu).
- **Data katalog dipisah ke file JS bersama**: **`assets/js/products.js`** (array `FILTERS` 8 kategori, `PRODUCTS` 44 produk: `category`, `image`, `name`, `desc`, `price`, `wa`, + map `PRODUCT_DESC` deskripsi cadangan per kategori) — dimuat `<script src="assets/js/products.js">` di `produk.html` **sebelum** `assets/js/cart.js` (WAJIB urut ini karena `cart.js` butuh `PRODUCTS`). Kartu & bar filter di `produk.html` dirender dari file ini. **Ganti nama/deskripsi/harga varian = edit `name`/`desc`/`price` di `assets/js/products.js`** (tambah produk = salin satu baris). Tombol kategori (bar + dropdown navbar + menu mobile + query `?filter=`) semua terhubung ke `data-category`/`data-filter` yang sama.
- **FITUR KERANJANG (checkout via WhatsApp) — sejak 2026-09-25, di `index.html` + `produk.html` (kode bersama `assets/js/cart.js`)**: tiap kartu katalog **dan** 3 kartu produk unggulan di beranda punya satu tombol penuh **"Tambah ke Keranjang"** (`data-add`); tombol "Pesan" (WA langsung per kartu) **DIHAPUS** 2026-09-25 atas permintaan pengguna.
  - **Kartu unggulan beranda = kategori, bukan varian**: `data-add` di `index.html` memakai id **`kardus-kategori` / `lakban-kategori` / `bubble-kategori`**, terdaftar di map **`KATEGORI_BY_ID`** (`assets/js/cart.js`, di bawah `PRODUCT_BY_ID`) berisi `image` (foto sampul), `name` ("Kardus (berbagai varian)"), `price` ("Mulai Rp …/satuan"). Semua pembacaan produk di `cart.js` memakai fungsi **`produkById(id)`** (varian dulu, lalu kategori) — jangan akses `PRODUCT_BY_ID` langsung. Tambah kartu unggulan = tambah 1 baris di `KATEGORI_BY_ID`.
  - **File**: `assets/js/cart.js` (371 baris) menyuntik seluruh markup panel/FAB/toast ke `<body>` — jangan tambah markup `#cart-panel`/`#cart-fab` di HTML, akan bentrok. **Posisi FAB** dibaca dari `data-cart-fab` pada `<body>`: `index.html` = `right` (kanan bawah, sejak 2026-09-26 tombol WA melayang dihapus), `produk.html` = `right`. Pemicu lain = elemen `.cart-trigger`: di `index.html` **sudah 0** (logo navbar & teks "Keranjang" di menu mobile keduanya dihapus 2026-09-26 — satu-satunya akses keranjang beranda = FAB mengambang, sama untuk desktop & HP), di `produk.html` 1 (navbar). Badge = kelas `.cart-badge` (hanya 1, di FAB).
  - **Data**: `localStorage` kunci **`awol_cart_v1`** (hanya `{ id, qty }`), **`awol_cart_outlet_v1`** (id outlet terpilih), **`awol_cart_pemesan_v1`** (`{ nama, wa, alamat }`). Ketiganya ditulis `JSON.stringify` → saat seeding/tes, nilai string harus di-`JSON.stringify` juga. `id` produk = **nama file gambar tanpa ekstensi** (`kardus-box`), dicocokkan ke `PRODUCTS` lewat `PRODUCT_BY_ID` → nama/harga selalu ikut berubah saat data diedit; item dengan id tak dikenal dibuang.
  - **WAJIL isi Data Pemesan**: `#cart-nama` (≥2 karakter), `#cart-wa` (9–15 digit setelah non-digit dibuang), `#cart-alamat` (≥5 karakter) + **WAJIL pilih outlet** (4 kartu dari `OUTLET_PILIHAN`: nama, kota, logo, nomor WA asli). Tanpa outlet tombol kirim tampil **"Lengkapi Data & Pilih Outlet"** (abu) + peringatan merah; outlet terpilih tapi pemesan kosong → `#cart-warn-pemesan` merah + border merah pada field yang salah; lengkap → tombol hijau WA **"Kirim Pesanan via WhatsApp"**. Isian pemesan tersimpan otomatis saat diketik (bisa pindah halaman).
  - **Pesan WA** dikirim ke nomor outlet terpilih (bukan nomor utama) dengan format: sapaan outlet → `1. Nama x qty satuan @ Rp harga/satuan` → `Total: n item` → `Subtotal estimasi: Rp ...` → blok **Data Pemesan (Nama/No. WhatsApp/Alamat)** → permintaan konfirmasi harga final/stok/ongkir.
  - **Harga**: `price` di `PRODUCTS` berformat "Mulai Rp 11.000/kg" → angka & satuan di-parse otomatis (`hargaAngka`, `hargaSatuan`). Varian **tanpa angka** ditampilkan apa adanya & subtotal jadi "Sesuai konfirmasi outlet". Karena formatnya "mulai", semua angka di panel ditulis sebagai **estimasi**.
  - Panel dibuka lewat `.cart-trigger`/FAB, ditutup lewat tombol X, overlay, atau `Esc`; `location.hash === '#keranjang'` membuka panel otomatis (sisa kompatibilitas). Toast `#cart-toast` mengonfirmasi aksi. Ada tombol "Kosongkan keranjang". Keranjang kosong menampilkan tombol **"Lihat Produk"** (`data-isi-katalog` → scroll `#kategori-produk` di katalog, atau pindah ke `produk.html#kategori-produk` dari beranda).
  - Diuji dengan jsdom: `C:\Users\Lenovo\AppData\Local\Temp\opencode\domtest\test-cart-shared.mjs` (146 tes, script eksternal disisip inline agar menguji isi file lokal; bukan bagian proyek). Jalankan: `node test-cart-shared.mjs` dari folder `domtest`.
- **Form kontak: TIDAK ADA lagi** (dihapus 2026-09-26). Section Kontak hanya tombol WA. Jangan tambahkan `#contact-form`/`#outlet-select`/`#kontak-wa-btn` kembali tanpa diminta; data pemesan sekarang hanya lewat panel keranjang.
- Email footer: adiwijayalanggeng@gmail.com.

## Panduan Edit Data (Cari Cepat di VS Code)
Cari penanda ini dengan `Ctrl+F` di `index.html`, `assets/js/products.js`, atau `assets/js/cart.js`. Nomor baris bisa bergeser jika ada edit, jadi cari kata kunci/komentar.

### Per Kategori Produk — 3 lokasi edit
Setiap kategori muncul di 3 tempat: **kartu slider** (index.html), **data varian** (`assets/js/products.js` array `PRODUCTS` — nama, deskripsi `desc`, harga sudah satu baris), & deskripsi kategori cadangan (`PRODUCT_DESC` di file yang sama, hanya dipakai bila varian tanpa `desc`). Harga harus diganti di index.html DAN `assets/js/products.js` agar konsisten.
Catatan (sejak 2026-09-24): `index.html` kini hanya menampilkan **3 produk unggulan** (Kardus, Lakban, Bubble Wrap). Foam, Plastik, Tali & Rakitan, dan Aksesoris hanya ada di `produk.html` — baris `PRODUCTS`/`PRODUCT_DESC` di `assets/js/products.js` tetap sumber utamanya.

| Kategori (cari: `<!-- … -->`) | index.html slider | `assets/js/products.js` `PRODUCTS` (varian) | Produk cadangan `PRODUCT_DESC` | Foto |
|---|---|---|---|---|
| **Kardus** | ~baris 169 (4 foto, "Mulai Rp 900") | 4 varian (baris 19–22) | desc kardus (baris 67) | `kardus-single-face`, `kardus-box`, `kardus-lembaran`, `kardus-honeycomb` |
| **Bubble Wrap** | ~baris 195 (6 foto, "Mulai Rp 650") | 6 varian (baris 23–28) | desc bubble (baris 68) | `bubble-roll`, `bubble-alumunium-foil`, `bubble-inflatable`, `bubble-meteran`, `bubble-potongan`, `bubble-mailer` |
| **Foam** | ~baris 223 (3 foto, "Mulai Rp 3.000") | 3 varian (baris 29–31) | desc foam (baris 71) | `foam-bounded`, `foam-bounded-2`, `foam-pe-sheet` |
| **Lakban** | ~baris 248 (11 foto, "Mulai Rp 1.500") | 11 varian (baris 32–42) | desc lakban (baris 69) | `lakban-OPP`, `lakban-fragile`, `lakban-double`, `lakban-double-tape-tebal-transparant`, `lakban-double-tape-foam`, `lakban-kertas`, `lakban-alumunium-foil`, `lakban-anti-bocor`, `lakban-clout-tape`, `lakban-police-line`, `isolasi-listrik` |
| **Plastik** | — (tidak lagi ditampilkan di index) | 12 varian (baris 43–54) | desc plastik (baris 70) | `plastik-wrapping`, `plastik-polymailer`, `plastik-klip`, `plastik-pouch`, `plastik-ziplock`, `plastik-clingwrap`, `plastik-hdpe`, `plastik-trash-bag`, `plastik-karung`, `plastik-shrink`, `plastik-vacum-embos`, `plastik-kresek` |
| **Tali & Rakitan** | ~baris 296 (2 foto, "Mulai Rp 18.000") | 2 varian (baris 55–56) | desc tali (baris 72) | `tali-straping-band`, `tali-rafia` |
| **Aksesoris Lainnya** | ~baris 320 (6 foto, "Mulai Rp 2.000") | 6 varian (baris 57–62) | desc aksesoris (baris 73) | `aksesoris-thermal-resi`, `aksesoris-thermal-roll`, `aksesoris-hvs`, `aksesoris-masker`, `aksesoris-hair-net`, `aksesoris-sarung-tangan-latex` |

### Data umum yang sering diedit
- **Nomor WhatsApp utama** `628211395050`: cari `wa.me/628211395050` di `index.html` (tombol "Chat WhatsApp Sekarang" section Kontak, hero, footer) dan `produk.html`.
- **Nomor per outlet**: ditulis langsung di `href` tiap markup — cari `wa.me/6285156338939`, `62895400927385`, `628998000502`, `6281220408070` di `index.html` (kartu outlet, dropdown navbar) dan footer. Keranjang memakai peta `OUTLET_PILIHAN` di `assets/js/cart.js` (baris ~10–15) — **ubah di kedua tempat bila nomor outlet berubah**.
- **Link Google Maps & Shopee per outlet**: kartu outlet (`index.html` cari `<!-- Outlet 1`) & footer. Kata kunci: `maps.app.goo.gl` & `shopee.co.id`.
- **Nama category/filter**: array `FILTERS` di `assets/js/products.js` (baris 7–16) + dropdown navbar & menu mobile file `index.html` (cari `data-filter=`) — ganti label di semua tempat agar konsisten.
- **Nama / deskripsi / harga varian**: cukup edit/salin satu baris `{ category: … }` di array `PRODUCTS` (`assets/js/products.js` baris 19–62) — field `name`, `desc`, `price` sudah satu baris per varian.
- **Keranjang & harga**: `id` item keranjang ikut nama file gambar (`p.image`) — `PRODUCT_BY_ID` dibangun otomatis dari `p.image` di `assets/js/cart.js` (baris ~18–19), jadi cukup ganti nama file + referensinya. Kartu unggulan beranda memakai id kategori manual (`kardus-kategori` dst, map `KATEGORI_BY_ID`) — **ganti harga "Mulai Rp" di `index.html` DAN di `KATEGORI_BY_ID` agar sama**. Varian tanpa angka harga (mis. "Hubungi Kami") tetap aman — subtotal otomatis jadi "Sesuai konfirmasi outlet".

## Konvensi Edit
- Edit di `index.html`, konsisten gaya Tailwind yang ada. Bahasa Indonesia.

## Yang Belum Dikerjakan / To-do
- [ ] Ganti nomor WhatsApp placeholder dengan nomor asli (produk per outlet bila perlu).
- [ ] Ganti link Google Maps dengan alamat/embed persis per outlet (alamat detail sudah terisi di kartu/kontak/JSON-LD; link masih share `maps.app.goo.gl`).
- [x] Isi alamat detail masing-masing outlet.
- [ ] Konfirmasi harga produk plastik baru (Klip, Pouch, Ziplock, Karung) & foam — saat ini diisi sementara (15.000–25.000).
- [ ] Konfirmasi harga & deskripsi produk (Kardus Honeycomb + varian Lakban) — sudah diupdate pengguna namun perlu validasi harga asli toko bila ada.
- [ ] Ganti testimoni dengan asli.
- [x] **Sosmed footer (ig/fb/yt) dihapus** (2026-09-25, atas permintaan pengguna — link `#` mati tidak ada akun asli).
- [x] Link Shopee per outlet sudah dipasang (kartu outlet, kontak, footer).
- [ ] **Tambahkan konversi Google Ads (AW-...)** — pasang `send_to` di blok `generate_lead` saat akun Google Ads sudah ada.
- [ ] Domain & SSL (`awolpackaging.com`) + validasi canonical/og:url/JSON-LD saat live. **Catatan**: GitHub Pages sudah menyediakan HTTPS gratis — bila domain didaftarkan tinggal set custom domain (Pages → Settings → Custom domain) + verifikasi DNS, lalu ubah URL canonical/og:url/sitemap/JSON-LD ke domain asli.
- [x] **`sitemap.xml`, `robots.txt`, dan `privacy.html` sudah dibuat** (2026-09-25) — tetapi URL masih placeholder `awolpackaging.com`: saat domain live, cek `lastmod` & pastikan path benar di hosting dan submit sitemap ke Google Search Console.
- [x] **OG image 1200×630 dibuat** (`og-1200x630.png`, 164 KB — banner gradasi oranye + logo CV + nama/tagline/4 Outlet • 44 Varian; dibangkitkan skrip System.Drawing di temp); `og:image` index & produk diarahkan ke sana + meta `og:image:type/width/height`.
- [x] **Keranjang pesanan (checkout via WhatsApp) di `produk.html` DAN `index.html`** — 2026-09-25 (v2): kode dipindah ke `assets/js/cart.js`, tombol "Pesan" per kartu dihapus, panel wajib diisi **Nama + No. WhatsApp + Alamat lengkap** (tersimpan di `localStorage`), tetap wajib pilih outlet. `privacy.html` sudah menambah catatan `localStorage` keranjang + data pemesan.
- [x] **Tombol tambah di 3 kartu produk unggulan `index.html`** — 2026-09-26 selesai: tombol "Pesan" diganti **"Tambah ke Keranjang"** (`data-add`), item kategori terdaftar di `KATEGORI_BY_ID` (`assets/js/cart.js`) sehingga bisa di-qty, subtotal, dan dikirim ke WA.
- [x] **Form kontak dihapus** (2026-09-26, atas permintaan pengguna) — section Kontak dipangkas dua kali, hasil akhir: blok oranye `hero-gradient` + 1 judul + 1 tombol "Chat WhatsApp Sekarang". Konsekuensi: event GA4 `generate_lead`/`lead_form` ikut hilang, `privacy.html` diperbarui (2026-09-26).
- [x] **Section CTA Banner dihapus** (2026-09-26, atas permintaan pengguna) — isinya duplikat dengan blok Kontak oranye; blok Kontak yang dipertahankan.

## File Logo (di folder proyek)
- `logo-CV.Adi-Wijaya-Langgeng.png` — logo CV (dipakai di navbar, hero, tentang, footer)
- `logo-awol-packaging-yogyakarta.png` — outlet AWOL Yogyakarta
- `logo-awol-packaging-sleman.png` — outlet AWOL Sleman
- `logo-adi-wijaya-packaging-klaten.png` — outlet ADI WIJAYA Klaten
- `logo-asia-lestari.png` — outlet ASIA LESTARI Madiun
- Semua logo di-resize ke maks. **512×512 px** (sejak 2026-09-24, dulu 4202×4202), PNG format ARGB (transparan). Dipanggil relatif dari `index.html`.
- **`logo-CV.Adi-Wijaya-Langgeng.png` diganti pengguna di VS Code (2026-09-24 ~19:57): kini 384×384 px, 93 KB** (logo utama di navbar, hero, tentang, footer, `og:image`). `favicon.png` diregenerasi dari logo terbaru.
- Latar belakang logo: abu muda (`bg-gray-200`), bentuk bulat/persegi sesuai konteks (rounded-full di outlet/kontak, rounded-lg/xl di navbar/hero, rounded-2xl di kartu tentang).
- `.vscode/settings.json` — konfigurasi ekstensi Live Server (port 5500, open di browser eksternal, fullReload) untuk preview live saat edit di VSCode.

## LOG SESI
- **2026-09-18** — Membuat landing page awal bernama AWOL Packaging (Desktop), lalu dipindah ke Documents\awol-packaging.
- **2026-09-18** — Rebrand menjadi CV. ADI WIJAYA LANGGENG: update hero, navbar, footer, tentang; tambah section **Outlet** (4 kartu: Yogyakarta, Sleman, Klaten, Madiun) dengan link Google Maps; form kontak mendapat field pilihan outlet; testimoni diberi konteks kota. Belum: nomor WA asli, alamat detail, harga asli, logo outlet, embed maps.
- **2026-09-18** — Tambah logo: CV dipasang di navbar, hero, kartu tentang, footer; logo 4 outlet dipasang di kartu outlet, daftar outlet di section tentang, dan daftar kontak. Logo dibuat bulat dengan latar putih + border (rounded-full, object-contain). Belum: nomor WA asli per outlet, alamat detail, harga asli, embed Google Maps, sosmed.
- **2026-09-18** — Ubah latar belakang semua logo (16 tempat) dari putih menjadi gradasi oranye→merah (`from-orange-500 to-red-600`). Semua logo kini tampil dengan latar gradasi di navbar, hero, outlet, tentang, kontak, dan footer.
- **2026-09-18** — Ubah lagi latar belakang logo menjadi abu muda (`bg-gray-200`) di 16 tempat sesuai permintaan. Gradasi oranye-merah dihapus.
- **2026-09-18** — Set up integrasi VSCode: install ekstensi Live Server + buat `.vscode/settings.json` (port 5500, open External Browser, fullReload) agar perubahan di VSCode tampil live di browser. Buat plugin global opencode (`~/.config/opencode/plugins/opencode-vscode.ts`) untuk auto-buka VSCode saat opencode mengedit file. Plugin perlu restart opencode agar aktif.
- **2026-09-18** — Siapkan struktur nomor per outlet: tombol "WhatsApp Outlet" hijau di 4 kartu outlet, kontak menampilkan nomor tiap outlet, dan form kontak otomatis mengirim ke nomor outlet yang dipilih via map `OUTLET_WA` di script. Nomor masih placeholder (Yogyakarta 628274000001, Sleman 628274000002, Klaten 628272000003, Madiun 628351000004) — menunggu nomor asli dari pengguna.
- **2026-09-18** — Pasang nomor WhatsApp asli per outlet: Yogyakarta 6285156338939, Sleman 62895400927385, Klaten 628998000502, Madiun 6281220408070 (kartu outlet, kontak, OUTLET_WA). Masih placeholder: nomor utama 6281234567890, alamat detail outlet, harga produk, embed Google Maps, sosmed.
- **2026-09-18** — Ubah tombol "Hubungi Kami" (navbar) & tombol WA melayang menjadi popup pilihan outlet: klik → tampil 4 logo outlet (Yogyakarta, Sleman, Klaten, Madiun), klik salah satu → buka WA nomor outlet tersebut. Tambah JS toggle dropdown (`wa-dropdown`, `float-wa-popup`) + tutup saat klik di luar.
- **2026-09-18** — Hapus 2 tombol CTA hero ("Pesan via WhatsApp" & "Lihat Outlet Kami") & ubah badge hero menjadi "Distributor Resmi Perlengkapan Packaging". Paragraf hero diubah manual pengguna ("Solusi meringankan HPP Packaging...").
- **2026-09-18** — Navbar: teks brand dibungkus background pill oranye (rounded-full), logo lalu dikembalikan di sampingnya. Hero: logo di H1 dihapus, dan grid 4 ikon (Kardus, Bubble Wrap, Lakban, Plastik) diganti panel gambar `backgroud.png` (rounded-3xl, overlay `bg-brand-900/30`). File `backgroud.png` (3,9 MB) ada di folder proyek.
- **2026-09-18** — Panel gambar background di hero disederhanakan: hilangkan overlay/rounded/shadow dan ganti jadi elemen `<img src="backgroud.png">` biasa. Karena `backgroud.png` berlatar transparan (ARGB), gambar kini tampak menyatu dengan gradasi oranye hero di area H1.
- **2026-09-18** — Ganti semua link Google Maps (12 tempat) dari pencarian nama outlet menjadi link share persis (`maps.app.goo.gl`) per outlet: Yogyakarta, Sleman, Klaten, Madiun — di kartu outlet, daftar kontak, dan footer.
- **2026-09-18** — Pindahkan section Produk ke atas section Outlet (urutan: Hero → Produk → Outlet → Keunggulan → Tentang → Kontak). Produk tetap `bg-gray-50` agar kontras dengan Outlet putih di bawahnya.
- **2026-09-18** — Beberapa penyempurnaan tampilan: navbar brand dibuat transparan (hapus `bg-gray-200` pada logo & pill oranye pada teks, teks jadi `text-gray-900`); gambar `backgroud.png` di hero diperbesar `scale-110`; padding vertikal hero dihapus agar mepet gambar; thumbnail 6 kartu produk diubah dari `h-48` menjadi `aspect-square` (1:1).
- **2026-09-21** — Ganti nomor WhatsApp utama `MAIN_WA` dari placeholder 6281234567890 menjadi asli 628211395050 (semua link wa.me di index.html: navbar, 6 kartu produk, CTA banner, form default). Nomor utama kini asli.
- **2026-09-21** — Tambah link toko Shopee per outlet di 12 tempat: kartu outlet (tombol "Toko Shopee" solid `bg-[#ee4d2d]` + ikon tas belanja), kontak (teks "Shopee (username) →" warna #ee4d2d), footer (link "· Shopee" oranye). Link: jogja `shopee.co.id/awolpackagingjogja`, sleman `awolpackagingsleman`, klaten `wijaya.packaging`, madiun `lestari.packaging`.
- **2026-09-22** — Pasang foto produk asli pada kartu Produk: emoji 📦 (Kardus) diganti slider 3 foto `kardus-1.png`, `kardus-2.png`, `kardus-3.png`; emoji 🧊 (Bubble Wrap) diganti slider 3 foto `bubble-1.png`, `bubble-2.png`, `bubble-3.png`. Foto tampil utuh (`object-contain`, `bg-gray-100`, padding), bisa digeser lewat tombol panah ‹ ›, titik navigasi, geser (swipe), dan autoplay 4 detik. Skrip slider reusable (`data-autoplay` opt-in) di akhir file. Kartu Lakban/Plastik/Tali/Aksesoris masih pakai emoji (belum ada foto).
- **2026-09-22** — Ubah tampilan slider foto produk: tiap foto kini tampil **full-bleed** (memenuhi seluruh area persegi kartu) — hapus `bg-gray-100`, padding, dan `object-contain`; ganti `w-full h-full object-cover`. Slider/geser/panah/titik/autoplay tetap berfungsi. Catatan: foto masih 100×100 px (blur saat di-crop jadi ~384 px), menunggu foto resolusi lebih besar dari pengguna.
- **2026-09-22** — Tambah fitur **"Lihat Semua Produk"** di bawah grid produk `index.html` → buka halaman katalog baru `produk.html`. Buat `produk.html`: navbar/hero/wave divider konsisten dengan index, section `#semua-produk` berisi 6 kartu foto produk (3 varian kardus + 3 varian bubble, `object-cover` full-bleed), tombol "Kembali ke Beranda", dan dropdown pilih outlet. Instruksi di kode: salin blok kartu untuk menambah produk baru.
- **2026-09-22** — Pengguna menambahkan foto baru (lakban ×4, plastik ×4, tali ×2) dan mengganti semua foto lama ke resolusi 384×384 px (blur 100 px selesai). Terapkan: kartu **Lakban, Plastik, Tali & Rakitan** di `index.html` diganti emoji → slider foto (kini 5 slider; Aksesoris masih emoji). `produk.html` ditambah 10 kartu katalog (total 16 kartu, 16 foto). Harga & link WA per kategori tetap placeholder kategori.
- **2026-09-22** — Rename halaman `produk.html` dari "Semua Produk" menjadi **"Kategori Produk"**: judul tab, menu navbar (desktop & mobile), badge hero, h1, section (id ganti `#kategori-produk`, h2 "Pilih Kategori Produk Anda"), dan tombol di index jadi "Lihat Kategori Produk". Tidak ada sisa referensi "Semua Produk".
- **2026-09-22** — Rename menyeluruh kata "Produk" → "Kategori Produk" (26 titik): menu index sekarang "Kategori Produk", label section "Kategori Produk Kami", "Pilih Kategori Produk", "Kategori Produk yang Diminati", "Jenis Produk"→"Kategori Produk", pesan WA "Kategori Produk:", footer, hero alt, dll. Section index `id="produk"` + anchor jadi `#kategori-produk` (3 href). Nama file `produk.html` tetap (harus, karena link & file asli).
- **2026-09-22** — Tambah **filter kategori interaktif** di `produk.html`: bar tombol (Semua/Kardus/Bubble Wrap/Lakban/Plastik/Tali & Rakitan), tiap kartu diberi `product-card` + `data-category`, JS toggle `.hidden` + tombol aktif oranye. Klik kategori → tampil hanya varian kategori itu (contoh: klik Kardus → tampil varian 1–3). Default "Semua". Semua 16 kartu ter-tag benar; belum ada kategori Aksesoris.
- **2026-09-22** — Pengguna menambahkan 3 foto Aksesoris Lainnya (`aksesoris-lainnya-1..3`, 384×384). Terapkan: kartu **Aksesoris Lainnya** di `index.html` ganti emoji 🏷️ → slider 3 foto (kini 6 slider, semua kategori punya foto, emoji tinggal 0); `produk.html` tambah tombol filter **"Aksesoris Lainnya"** (data-filter="aksesoris") + 3 kartu varian (total 19 kartu, harga "Bervariasi").
- **2026-09-22** — Tab **"Kategori Produk"** di navbar `produk.html` jadi dropdown (`#kategori-btn` + `#kategori-dropdown`): klik → tampil daftar nama kategori (Semua, Kardus, Bubble Wrap, Lakban, Plastik, Tali & Rakitan, Aksesoris Lainnya); klik salah satu → filter otomatis (`btn.click()` ke `.cat-btn` sesuai data-filter) + smooth scroll ke `#kategori-produk`. Menu mobile juga mendapat 7 link kategori (indent). Tutup saat klik di luar.
- **2026-09-22** — Dropdown **"Kategori Produk"** juga dipasang di halaman utama (`index.html`): tab navbar desktop + 7 link di menu mobile. Setiap item menu mengarah ke `produk.html?filter=<kategori>#kategori-produk`. `produk.html` kini membaca query `?filter=` saat load (`URLSearchParams`) → otomatis klik `.cat-btn` sesuai filter dan scroll ke section. Tutup saat klik di luar.
- **2026-09-22** — **Refactor `produk.html` menjadi data-driven**: grid statis 19 kartu & bar filter diganti kontainer `#product-grid` + `#filter-bar` yang di-render JS dari konfigurasi di atas script (`FILTERS`, `PRODUCTS` 19 entri, `PRODUCT_DESC`). Tujuannya agar **ganti nama varian mudah**: cukup edit `name:` di array `PRODUCTS`; tambah produk tinggal salin baris. Semua fungsi lain (filter, dropdown navbar, auto-filter query) tetap berjalan.
- **2026-09-22** — Tambah bagian **"Panduan Edit Data (Cari Cepat di VS Code)"** di AGENTS.md (tabel per kategori: lokasi slider index.html, baris `PRODUCTS`/`PRODUCT_DESC` di produk.html, file foto, harga) + daftar data umum (MAIN_WA, OUTLET_WA, link Maps/Shopee, FILTERS). Koreksi jumlah varian produk.html menjadi 19 (termasuk Aksesoris). Catatan: `bubble-4.png` & `plastik-5.png` belum terpakai.
- **2026-09-22** — Tambah field **`desc` per varian** di array `PRODUCTS` (produk.html, 19 baris) — deskripsi tiap produk kini bisa diedit langsung satu baris (`name`/`desc`/`price`). Render kartu pakai `p.desc || PRODUCT_DESC[p.category]` (PRODUCT_DESC jadi cadangan saja). Update Panduan Edit Data & Logika Penting di AGENTS.md.
- **2026-09-22** — Pengguna mengedit manual di VS Code: semua varian diberi nama & harga asli (Kardus: Single Face/Box/Lembaran; Bubble: Roll Utuh/Meteran/Potongan/Amplop Bubble Mailer 650–90.000; Lakban: OPP/Fragile/Double Tape dll 1.500–7.000; Plastik: Wrapping/Polymailer/Food Grade/HDPE/Trash Bag/Kresek 160–25.000; Tali: Strapping Band/Rafia; Aksesoris: Thermal Resi/Thermal Roll/HVS) + deskripsi `PRODUCT_DESC` diperbarui. **Terapkan produk baru**: `bubble-4.png` & `plastik-5.png` (bubble jadi 4 varian, plastik jadi 6 varian — total 22). **Sinkronkan `index.html`**: 6 kartu kategori diupdate deskripsi & harga "Mulai Rp" terkecil (900/650/1.500/160/18.000/15.000). Catatan: `plastik-5.png` dipakai 2× (Trash Bag & Kresek); typo "SHigienis" di desc Plastik Food Grade produk.html. Update AGENTS.md (jumlah 22, tabel Panduan, log).
- **2026-09-23** — Rename semua file foto produk (22 file) sesuai nama varian agar di "database" tercantum nama produk: `kardus-1..3`→`kardus-single-face/box/lembaran`, `bubble-1..4`→`bubble-roll/meteran/potongan/mailer`, `lakban-1..4`→`lakban-opp-12-24/opp-45-48/fragile/serbaguna`, `plastik-1..5`→`plastik-wrapping/polymailer/food-grade/hdpe/trash-bag` (`plastik-trash-bag` tetap dipakai 2× utk Trash Bag & Kresek sesuai pilihan pengguna), `tali-1..2`→`tali-straping-band/rafia`, `aksesoris-lainnya-1..3`→`aksesoris-thermal-resi/thermal-roll/hvs`. Referensi di `index.html` (slider 6 kartu) & `produk.html` (field `image:` array `PRODUCTS`) sudah disinkronkan — pengecekan `Ctrl+F` nama lama tidak ada sisa. Update AGENTS.md (daftar foto & tabel Panduan).
- **2026-09-23** — Tambah kategori **Foam** + produk baru sesuai gambar yang pengguna berikan. Pengguna menambahkan file: `Foam-Bounded1`, `Foam-Bounded-2`, `PE-Foam-Sheet` (di-rename jadi `foam-bounded`, `foam-bounded-2`, `foam-pe-sheet`), `plastik-srink`, `plastik-vacum-embos`, `plastik-kresek`, `plastik-clingwrap` (menggantikan `plastik-food-grade`), `aksesoris-masker`, `aksesoris-hair-net`, `aksesoris-sarung-tangan-latex`. Terapkan: **index.html** — kartu slider Foam baru (7 slider), dropdown navbar desktop+mobile+form dpt opsi Foam, slider Plastik ditambah foto srink/vacuum-embos/kresek & perbaiki ref clingwrap, slider Aksesoris ditambah 3 foto (6 total), harga "Mulai Rp" aksesoris 15.000→2.000. **produk.html** — `FILTERS` tambah foam, `PRODUCTS` jadi 30 (foam 3: Bounded/Bounded Lembaran/PE Foam Sheet; plastik +2: Shrink/Vacuum Embos; kresek ganti foto `plastik-kresek`; aksesoris +3: Masker/Hair Net/Sarung Tangan Latex), typo "SHigienis"→"Higienis", `PRODUCT_DESC` tambah foam, dropdown navbar+mobile +Foam. Harga foam/plastik baru masih placeholder. Update AGENTS.md (jumlah 30, tabel Panduan, log).
- **2026-09-24** — Tambah produk baru sesuai gambar yang pengguna masukkan ke folder (`kardus-honeycomb`, `isolasi-listrik`, dan 10 lakban baru; gambar `lakban-opp-12-24`, `lakban-opp-45-48`, `lakban-serbaguna` lama sudah DIHAPUS dari folder). **produk.html** — `PRODUCTS` jadi 38 (kardus +1: Honeycomb; lakban 4→11: OPP Clear & Tan, Fragile, Double Tape, Double Tape Tebal Transparan, Double Tape Foam, Kertas/Masking, Alumunium Foil, Anti Bocor, Clout/Cloth Tape, Police Line, Isolasi Listrik; 2 varian OPP lama digabung jadi satu). `PRODUCT_DESC.lakban` diperbarui. **index.html** — slider Kardus +foto honeycomb (4 slide), slider Lakban dipasang 11 foto baru, deskripsi kartu Kardus & Lakban diupdate. Deskripsi & harga produk baru diisi dari pengetahuan umum (Kardus Honeycomb Rp 6.000/lembar; lakban beragam 1.500–12.000/roll) — **perlu diganti harga asli toko**. Referensi gambar lama sudah tidak ada sisa; semua 38 image terverifikasi ada di folder. Update AGENTS.md (jumlah 38, tabel Panduan, to-do, log).
- **2026-09-24** — Halaman utama `index.html` hanya menampilkan **3 produk unggulan** (urutan: Kardus, Lakban, Bubble Wrap). Kartu Foam, Plastik, Tali & Rakitan, dan Aksesoris dihapus dari grid index (tetap ada lengkap di `produk.html`). Judul section: badge "Produk Unggulan Kami", h2 "3 Produk Unggulan", paragraf ditambah arah ke halaman Kategori Produk. Tombol "Lihat Kategori Produk" tetap mengarah ke `produk.html`. Update AGENTS.md (struktur, catatan tabel Panduan, log).
- **2026-09-24** — Tambah **SEO & data struktur**: meta description, keywords, favicon, Open Graph, dan **JSON-LD LocalBusiness** (1 Organization + 4 Store) di `<head>` `index.html` & `produk.html`. **Alamat lengkap 4 outlet** (diberikan pengguna: Yogyakarta Jl. Tunggul Nogo 362A, Sleman Jl. Kaliurang Timur, Klaten Jl. Dr. Cipto Mangun Kusumo, Madiun Jl. Cokrobasonto 47) ditampilkan di kartu outlet & section kontak. **Footer**: nomor placeholder 0812-3456-7890 diperbaiki jadi link WA. Email footer masih placeholder. Belum: domain asli (`og:url`, `url` JSON-LD), embed maps, testimoni asli, sosmed footer. Update AGENTS.md (alamat, to-do, log).
- **2026-09-24** — Ganti placeholder footer di `index.html`: nomor tampilan diperbaiki jadi **0821-1395-050** (format benar dari WA 628211395050, link tetap `wa.me/628211395050`) dan email jadi **adiwijayalanggeng@gmail.com** (placeholder `info@adiwiyayalanggeng.com` dihapus). Update AGENTS.md (Logika Penting, log).
- **2026-09-24** — Pasang **placeholder domain `awolpackaging.com`** (pilihan pengguna, belum terdaftar/hosting): `og:url`, `<link rel="canonical">`, dan `url`+`@id` Organization di JSON-LD. `index.html` -> root `/`, `produk.html` -> `/produk.html`. Saat domain aktif cukup validasi ulang (canonical & JSON-LD masih dengan path benar). Update AGENTS.md (Logika Penting, log).
- **2026-09-24** — **Kompres semua 44 gambar** (total ~17,7 MB → ~3,2 MB, hemat ~82%): 38 foto produk 384×384 diubah **PNG → JPG** (kualitas 80, hemat 89–93% per file) + semua referensi `image:` di `produk.html` (.png'→.jpg') dan `src="*.png"` per nama produk di `index.html` (logo/backgroud tidak ikut diganti). 5 logo di-resize **4202×4202 → maks 512×512 px** (PNG transparan, hemat 88–96%). `backgroud.png` di-resize **1654 → 1200×1200 px** (3,9 MB → 2,1 MB, PNG transparan). Backup semua PNG asli di `C:\Users\Lenovo\AppData\Local\Temp\opencode\awol_images_backup\`. Verifikasi: 0 referensi rusak, JSON-LD valid. **Catatan teknis**: PowerShell 5.1 `Get-Content -Raw` membaca UTF-8 tanpa BOM sebagai ANSI → karakter non-ASCII sempat rusak (mojibake `â€"`) pada 2 file; sudah diperbaiki dengan reverse-encoding CP1252→UTF-8 (em-dash/· normal kembali, tanpa BOM). Update AGENTS.md (Struktur foto: JPG + logo 512px, log).
- **2026-09-24** — **Favicon baru**: buat `favicon.png` (300×300, ~30 KB) = logo CV ditimpa di atas **latar putih** (padding ~25px, logo ~250px) agar ikon tab browser tidak transparan. `<link rel="icon">` di `index.html` & `produk.html` diarahkan dari `logo-CV.Adi-Wijaya-Langgeng.png` → `favicon.png`. Logo asli (transparan) tetap dipakai di navbar/hero/tentang/footer dan `og:image`. Update AGENTS.md (log).
- **2026-09-24** — Tombol bawah section produk `index.html`: teks **"Lihat Kategori Produk" → "Lihat Semua Produk"** + tambah `target="_blank" rel="noopener"` agar membuka `produk.html` **di tab baru**. Update AGENTS.md (Info Dasar, Struktur, log).
- **2026-09-24** — **Section Tentang (index.html ~baris 577)**: logo `logo-CV.Adi-Wijaya-Langgeng.png` di kotak gradasi dibuat **transparan** (hapus `bg-gray-200`, `rounded-2xl`, `shadow-lg`, `p-2`) dan tagline di bawahnya diganti menjadi **"Partner Packaging Bisnis Anda"**. Baris atribusi "— Tim CV. ADI WIJAYA LANGGENG" dihapus (terikat quote lama sehingga tak relevan lagi). Update AGENTS.md (log).
- **2026-09-25** — **Deteksi perubahan manual pengguna di VS Code**: `logo-CV.Adi-Wijaya-Langgeng.png` diganti versi **384×384 px** (93 KB, ARGB). `favicon.png` **diregenerasi** dari logo terbaru agar ikon tab konsisten (300×300, 39,8 KB). Update AGENTS.md (File Logo + log).
- **2026-09-25** — **Bersihkan salinan lama proyek** yang jadi sumber kebingungan "kode berbeda dengan landing page": folder `C:\Users\Lenovo\Desktop\awol-packaging` (memuat `index.html` & `AGENTS.md` versi 18-Sept, WA placeholder, produk tanpa foto) **DIHAPUS** atas permintaan pengguna. Verifikasi: `lakban-opp-12-24`/`lakban-opp-45-48` dan nama produk "12mm/24mm/45mm/48mm" tidak ada di disk manapun (proyek, Desktop, Downloads, backup temp). VS Code di-reopen ke folder yang benar `C:\Users\Lenovo\Documents\awol-packaging`; jika tab lama masih muncul, tutup tab lalu buka ulang, dan hard refresh browser (Ctrl+F5).
- **2026-09-25** — **Deteksi edit manual pengguna di VS Code**: kategori **Plastik +4 produk** (`plastik-klip`, `plastik-pouch`, `plastik-ziplock`, `plastik-karung` — Klip/Pouch/Ziplock Rp 15.000/roll, Karung Rp 25.000/pack) → `produk.html` jadi **42 varian** (plastik 8→12). Foto baru berformat **PNG** (~244–295 KB, belum terkompresi JPG seperti 38 foto lain). `index.html` tidak berubah (plastik tidak tampil di halaman utama). Update: AGENTS.md (Info Dasar 42, Logika Penting, Panduan Edit table plastik, to-do harga), meta description `produk.html` "38"→"42 produk". To-do berikutnya: konversi 4 PNG baru → JPG bila ingin konsisten ukuran.
- **2026-09-25** — **Konversi 4 foto plastik baru → JPG** (atas persetujuan pengguna): `plastik-klip/pouch/ziplock/karung` PNG (384×384, 244–295 KB) diubah ke **JPG kualitas 80** (19–25 KB) via System.Drawing, referensi `image:` di `produk.html` di-update `.png`→`.jpg`, PNG asli dihapus dari proyek & di-backup ke `C:\Users\Lenovo\AppData\Local\Temp\opencode\awol_images_backup\`. Semua 42 foto produk kini konsisten JPG 384×384.
- **2026-09-25** — **Deteksi edit manual pengguna ke-2 di VS Code**: kategori **Bubble Wrap +2 produk** (`bubble-alumunium-foil` — Alumunium Foil Rp 18.000/roll, `bubble-inflatable` — Inflatable/Poly Air Rp 2.500/roll) → `produk.html` jadi **44 varian** (bubble 4→6, urutan: Roll Utuh, Alumunium Foil, Inflatable, Meteran, Potongan, Mailer). 2 foto baru dikonversi **PNG → JPG** (384×384, kualitas 80, 24–29 KB; PNG dibackup ke temp `awol_images_backup`), referensi `.png`→`.jpg`, meta description "42"→"44 produk". `index.html` tidak berubah (slider Bubble tetap 4 foto lamanya; varian baru hanya muncul di katalog). Update AGENTS.md (Info Dasar 44, Logika Penting, Panduan Edit table bubble, log). Semua 44 foto kini konsisten JPG 384×384.
- **2026-09-25** — **Optimasi performa & konversi untuk Google Ads (sesi analisis landing page)**: (1) **Tailwind CDN dikompilasi ke CSS statis** `assets/css/style.css` (21 KB, minify) via Tailwind standalone CLI v3.4.17 + `tailwind.config.js` + `assets/css/input.css` — script CDN, `tailwind.config` inline, dan `<style>` (hero-gradient + smooth scroll) dihapus dari `<head>` kedua file, diganti `<link rel="stylesheet" href="assets/css/style.css">`. (2) **gtag.js dipasang** (placeholder `G-XXXXXXXXXX`, 2 baris per file) + **event konversi `generate_lead`** (delegasi klik `wa.me`/`shopee.co.id` di index & produk, plus submit form kontak). (3) **backgroud.png 2,19 MB → 1,25 MB** (1200→900 px, PNG tetap transparan; asli di-backup ke temp) + `loading="lazy"` (di HP gambar tidak ikut ter-download). (4) **Lazy loading** semua 19+1 foto slider index & template kartu `produk.html`. (5) **Hero CTA baru**: "Chat WhatsApp" + "Lihat Kategori Produk". (6) Perbaikan statistik: "50+ Kategori Produk" → **"44 Varian Produk"**. Verifikasi: 0 referensi CDN/config lama, semua kelas penting ada di style.css (bg-brand-600, hero-gradient, -translate-y-1/2, border-white/80, dst). **Belum selesai pada sesi itu**: domain/SSL; sitemap/robots/privacy; OG 1200×630. Kemudian pada sesi yang sama ID GA4 asli **`G-N42Q7Z749M`** dipasang dari Property ID 555927985 atas informasi pengguna (placeholder G-XXXXXXXXXX diganti di 2 baris × 2 file). File baru di proyek: `tailwind.config.js`, `assets/css/input.css`, `assets/css/style.css`.
- **2026-09-25** — **Lanjutan optimasi Google Ads (item #2 audit, sosmed footer dihapus)**: (1) **Tracking dipisah** di index & produk — klik `wa.me` = `generate_lead` (`event_label: wa_click`), klik `shopee.co.id` = event terpisah `outbound_click` (`transport_type: beacon`) agar konversi WA tidak terkontaminasi klik Shopee. (2) **Satuan harga** ditambah di kartu Produk Unggulan index: "Mulai Rp 900/pcs", "Rp 1.500/roll", "Rp 650/pcs". (3) **JSON-LD Organization** ditambah `logo`, `image`, dan `contactPoint` (sales, ID) di index & produk. (4) **File SEO baru**: `sitemap.xml` (3 URL) & `robots.txt` (Allow semua + Sitemap) — URL masih placeholder `awolpackaging.com`. (5) **`privacy.html`** (Kebijakan Privasi & Syarat: data formulir dikirim via WA tanpa simpan server, cookie GA4 `G-N42Q7Z749M`, hak pengguna, syarat pemesanan) + link di footer `index.html`; `privacy.html` ditambahkan ke `content` `tailwind.config.js` → **CSS di-rebuild**; kelas baru terverifikasi ada di style.css. (6) **Blok sosmed footer (ig/fb/yt `href="#"`) dihapus** atas permintaan pengguna. Verifikasi: 0 referensi sirik; format "Mulai Rp …" ada satuan di 3 produk unggulan. **Belum selesai**: testimoni asli, AW-... send_to, domain/SSL + submit sitemap ke GSC, OG 1200×630.
- **2026-09-25** — **(1) OG image 1200×630 dibuat** `og-1200x630.png` (164 KB: gradasi oranye + panel putih + logo CV + "CV. ADI WIJAYA LANGGENG / AWOL Packaging · Partner Packaging Bisnis Anda / 4 Outlet • 44 Varian Produk • Grosir & Ecer / awolpackaging.com", font auto-fit) via skrip `C:\Users\Lenovo\AppData\Local\Temp\opencode\make-og.ps1`. `og:image` di `index.html` & `produk.html` diarahkan dari logo → `og-1200x630.png`, ditambah `og:image:type/width/height`. **(2) Harga semua produk dikonfirmasi benar oleh pengguna** (tidak ada perubahan). **Belum selesai**: testimoni asli (keputusan pengguna 2026-09-25: **dibiarkan dulu**), AW-... `send_to`, domain/SSL + submit sitemap ke Search Console.
- **2026-09-25** — **Optimalisasi gambar hero agar cepat tapi tetap bagus**: `backgroud.png` (1.224 KB, 900×900) dikonversi ke **WebP** via Google cwebp (diunduh ke temp `libwebp-1.4.0-windows-x64`): `backgroud.webp` 900px = **115 KB**, `backgroud-600.webp` 600px = **61 KB**; fallback `backgroud-600.png` (571 KB). Hero `index.html` diganti `<picture>` + `srcset`/`sizes` (HP ambil 600w, desktop 45vw→900w) + `<link rel="preload imagesrcset/imagesizes fetchpriority=high>` di `<head>`; `loading="lazy"` DIHAPUS dari hero image (LCP harus eager, diberi `fetchpriority="high"`). Commit `aface7c`, terverifikasi live: HTML termuat `<picture>`, semua varian 200 (115/61/571 KB). Update situs: commit+push otomatis ter-deploy.
- **2026-09-25** — **Perbaikan tampilan HP**: gambar hero `backgroud.png` yang tadinya hanya desktop (`hidden lg:block`, dipasang saat optimasi performa agar HP tidak unduh PNG) kini tampil di **semua ukuran layar** (`class="block"`, index.html hero 2nd column) atas permintaan pengguna. Commit `aef69d7`, sudah ter-verifikasi live di GitHub Pages. Update situs = tetap `git add`+`commit`+`push` (Pages auto-deploy).
- **2026-09-25** — **Deploy ke GitHub Pages**: install GitHub CLI via winget (v2.101.0). Autentikasi pakai PAT pengguna — token kurang scope `read:org` sehingga `gh auth login` gagal; dipakai via `GH_TOKEN` (API jalan). **Git repo di-init** (branch `main`), identitas commit set ke noreply GitHub `<333701987+awpackagingklaten-prog@users.noreply.github.com>` (root commit `d957d19`, 62 file). Repo **publik** dibuat (`github.com/awpackagingklaten-prog/awol-packaging`), kredensial disimpan di Windows Credential Manager (bukan di URL). **GitHub Pages aktif** → `https://awpackagingklaten-prog.github.io/awol-packaging/` (build status "built"; index, produk, privacy, sitemap, robots, CSS, PNG semuanya HTTP 200; gtag `G-N42Q7Z749M` termuat). **Catatan**: canonical/og:url/JSON-LD `@id` masih `awolpackaging.com` (placeholder) — saatan domain asli dipasang: set custom domain Pages + CNAME + verifikasi DNS, ubah canonical/og/sitemap/JSON-LD, submit sitemap ke Google Search Console. Repo sengaja publik karena Pages free plan tidak mendukung private.
- **2026-09-25** — **Tambah fitur KERANJANG (checkout via WhatsApp)** di `produk.html` atas permintaan pengguna: 44 kartu katalog dapat tombol "Keranjang" (`data-add`) berdampingan tombol "Pesan" (WA langsung, dipertahankan); panel keranjang geser (`#cart-panel`) berisi daftar item (qty −/+ hapus), subtotal estimasi, dan **pilihan outlet WAJIB** (4 kartu + logo) — tombol kirim tampil "Pilih Outlet Dulu" (abu + peringatan) sampai outlet dipilih, lalu hijau "Kirim Pesanan via WhatsApp" dan pesan dikirim ke **nomor WA outlet terpilih** dengan daftar item, qty, harga satuan, total item, dan subtotal estimasi. Data keranjang di `localStorage` (`awol_cart_v1` = `{id, qty}` dengan `id` = nama file gambar, `awol_cart_outlet_v1` = id outlet) sehingga tetap ada saat pindah halaman; `index.html` hanya dapat ikon keranjang + badge jumlah yang menuju `produk.html#keranjang` (panel terbuka otomatis via hash) + link di menu mobile. Harga non-angka aman (ditampilkan apa adanya). Event GA4 baru **`add_to_cart`**; `generate_lead` tetap otomatis saat kirim (link `wa.me` outlet). `privacy.html` ditambah catatan keranjang `localStorage` + `add_to_cart`. Tailwind di-rebuild (`style.css` 24.595 byte; kelas baru `sm:max-w-md`, `z-[60/70/80]`, `min-w-[18px]/[20px]`, `text-[10px]/[11px]`, `bg-[#25D366]`, `ring-brand-600` terverifikasi ada). **Pengujian**: 31 tes fungsional + 5 tes GA4 via jsdom (Node v24, jsdom di `Temp\opencode\domtest\`) — semua lulus; 1 bug ditemukan & diperbaiki (tombol kirim tetap aktif saat keranjang kosong). **Belum dikerjakan**: tombol tambah di 3 kartu unggulan `index.html` (butuh harga varian), `send_to` AW-..., dan domain/SSL. **Sudah live**: commit `5aa3da4` push ke `main` (Pages auto-build, verifikasi `produk.html`/`index.html`/`privacy.html`/`style.css` HTTP 200 + marker keranjang termuat).
- **2026-09-25** — **Refactor keranjang v2 atas permintaan pengguna** (3 permintaan: hapus tombol "Pesan", keranjang di halaman utama, data pemesan baru). (1) Kode & data dipisah jadi 2 file baru: **`assets/js/products.js`** (75 baris: `FILTERS` 8, `PRODUCTS` 44, `PRODUCT_DESC` 7 — hasil ekstraksi dari inline `produk.html`, em-dash utuh) & **`assets/js/cart.js`** (360 baris: markup panel/FAB/toast di-inject ke `<body>`,+qty, outlet, pemesan, WA, toast, GA4). `produk.html` turun 496→426 baris; semua `<script>` inline keranjang & array data dihapus. (2) Kartu katalog kini 1 tombol penuh **"Tambah ke Keranjang"** (`w-full`, ikon plus) — link WA "Pesan" per kartu dihapus seluruhnya (0 dari 44). (3) `index.html` mendapat keranjang yang sama: `<body data-cart-fab="left">` → FAB **kiri bawah** (`left-6`, tombol WA melayang tetap `right-6`), link `#keranjang` jadi 2 elemen `.cart-trigger` `<button>` (navbar + mobile), badge lama `.cart-nav-badge` + IIFE badge dihapus, script shared dimuat sebelum script inline. (4) Panel kini mewajibkan **Nama (≥2) + No. WhatsApp (9–15 digit) + Alamat lengkap (≥5)** — `#cart-nama`/`#cart-wa`/`#cart-alamat`, disimpan di `localStorage` `awol_cart_pemesan_v1` (ikut saat pindah halaman), border merah + `#cart-warn-pemesan` bila kurang; blok "Data Pemesan" masuk ke pesan WA. Tombol kirim: "Keranjang Kosong" → "Lengkapi Data & Pilih Outlet" → "Kirim Pesanan via WhatsApp" (hijau). Kanal `awol_cart_outlet_v1` = `JSON.stringify(id)`. (5) `tailwind.config.js` `content` += `./assets/js/*.js` → rebuild `style.css` 25.670 byte (kelas baru `max-h-[75vh]`, `resize-none`, `border-red-400`, `focus:ring-red-400`, `left-6` terverifikasi). (6) `privacy.html` catatan keranjang ditulis ulang (nama/WA/alamat + penghapusan). **Pengujian**: `Temp\opencode\domtest\test-cart-shared.mjs` — **130 tes jsdom lulus 100%** (katalog: 44 kartu & 0 link wa.me, add/qty/subtotal/hapus, validasi 5 skenario, isi pesan WA + nomor outlet, hash `#keranjang`, panel buka/tutup; beranda: FAB kiri, badge & isi keranjang dari localStorage, form terisi, kirim ke WA Sleman; aset: 44 foto + logo outlet ada). Tes lama `test-cart.mjs`/`test-gtag.mjs` dihapus (uji implementasi lama). **Belum dikerjakan**: tombol tambah di 3 kartu unggulan beranda (butuh harga varian), `send_to` AW-..., domain/SSL.
- **2026-09-26** — **Beranda: tombol tambah di kartu unggulan + section Kontak disederhanakan** (2 permintaan pengguna). (1) `index.html` — tombol "Pesan" (WA langsung) di 3 kartu Produk Unggulan (Kardus, Lakban, Bubble Wrap) diganti **"Tambah ke Keranjang"** (`data-add="kardus-kategori" | "lakban-kategori" | "bubble-kategori"`, gaya identik kartu katalog). Supaya item kategori bisa di-qty, masuk subtotal, dan dikirim ke WA, `assets/js/cart.js` menambah map **`KATEGORI_BY_ID`** (foto sampul + nama "… (berbagai varian)" + harga "Mulai Rp …") dan fungsi **`produkById(id)`** (varian dulu, lalu kategori); semua pembacaan produk diganti ke fungsi itu dan `addToCart` bail-out bila id tak dikenal. (2) Section Kontak dipangkas jadi blok `max-w-3xl` terpusat: judul **"Butuh Packaging dalam Jumlah Besar?"** + teks jam operasional + tombol **"Hubungi Outlet"** (dropdown 4 outlet bermerkah, `#kontak-wa-btn` / `#kontak-wa-dropdown`, toggle + tutup saat klik luar) + tombol **"Chat WhatsApp Sekarang"** (WA utama). **Dihapus total**: form kontak 5 field, daftar info 4 outlet di kontak, blok jam operasional, handler `submit` form + konstanta `MAIN_WA`/`OUTLET_WA` (105 baris HTML + 26 baris JS); `index.html` 1043 → 986 baris. (3) `privacy.html`: bullet "Data formulir kontak" + sebutan formulir (3 tempat) dihapus, tanggal "Terakhir diperbarui" → 26 September 2026. (4) Tailwind di-rebuild (`style.css` 24.786 byte; kelas baru `max-w-3xl`, `left-1/2`, `-translate-x-1/2`, `border-brand-600`, `hover:bg-brand-50`, `sm:flex-row`, `py-3.5`, `text-left` terverifikasi ada). **Pengujian**: `Temp\opencode\domtest\test-cart-shared.mjs` — **146 tes jsdom lulus 100%** (tes lama "kartu unggulan tidak punya tombol tambah" diganti tes 3 tombol + toast + subtotal + isi panel + dropdown kontak buka/tutup). **Catatan insiden**: saat menyplice section Kontak lewat PowerShell, `List[string].AddRange` gagal diam-diam sehingga `index.html` sempat terpotong jadi 47 baris (dipulihkan `git checkout -- index.html`, 3 tombol di-edit ulang); `AGENTS.md` sempat terpotong juga saat menambah log (dipulihkan, log ditulis ulang). **Hindari**: pakai `[string[]]$a + [string[]]$b` bukan `List.AddRange`, dan cek `(Get-Item <file>).Length` setelah menulis. **Belum dikerjakan**: commit + push (menunggu persetujuan pengguna), `send_to` AW-…, domain/SSL.
- **2026-09-26** — **Section Kontak dipangkas jadi blok oranye saja** (permintaan lanjutan pengguna: "hapus semuanya, hanya yang latar belakang oren"). `index.html` baris 698–708 (versi sebelumnya) diganti: `<section id="kontak" class="py-20 sm:py-28 hero-gradient">` → satu `<h2>` "Lokasi Anda Jauh dari Outlet - Outlet Kami? Namun Butuh Packaging dalam Jumlah Besar? Semua Solusi Ada di Kami..!!" (`text-3xl sm:text-4xl font-extrabold text-white mb-8`, `max-w-4xl`) + satu `<a>` "Chat WhatsApp Sekarang" (`bg-white text-brand-700 px-8 py-4`, teks pra-filled: "Halo CV. Adi Wijaya Langgeng, saya berada jauh dari outlet, namun butuh packaging dalam jumlah besar"). **Dihapus**: badge "Hubungi Kami", sub-teks jam operasional, tombol "Hubungi Outlet" + dropdown 4 outlet, serta toggle JS `#kontak-wa-btn`/`#kontak-wa-dropdown` (penting: harus ikut dihapus, kalau tidak `addEventListener` pada null menghentikan seluruh script inline). `index.html` 986 → 935 baris. Tailwind di-rebuild (`style.css` 24.731 byte). **Pengujian**: `test-cart-shared.mjs` — **145 tes lulus** (tes dropdown Kontak diganti 6 tes struktur baru: teks, tombol, `hero-gradient`, tidak ada form, tidak ada sisa `kontak-wa`). **Catatan**: section CTA Banner tepat di bawah Kontak punya pesan & tombol hampir identik — sudah dicatat di To-do, menunggu keputusan pengguna. **Belum dikerjakan**: commit + push (menunggu persetujuan pengguna).
- **2026-09-26** — **Section CTA Banner dihapus** (permintaan pengguna: "hapus yang bagian bawah"). 13 baris `<!-- CTA BANNER -->` + `<section class="py-16 hero-gradient">` di `index.html` dibuang; blok Kontak oranye (yang barusan dibuat) tetap menjadi satu-satunya CTA penutup sebelum footer. `index.html` 935 → **912 baris**; `hero-gradient` di `index.html` tersisa 2 (kartu hero + blok kontak). Verifikasi: `CTA BANNER` 0, "Butuh Packaging dalam Jumlah Besar" 1, section 8/8, div 150/150. Tes `test-cart-shared.mjs` **145 lulus**. AGENTS.md: Struktur (nomor section 10–11 + catatan urutan), To-do (CTA Banner → selesai). **Belum dikerjakan**: commit + push (menunggu persetujuan pengguna).
- **2026-09-26** — **Tombol WhatsApp melayang dihapus, FAB keranjang pindah ke kanan bawah, logo keranjang navbar dihapus** (3 permintaan pengguna di `index.html`). (1) Blok `<!-- FLOATING WHATSAPP BUTTON + POPUP OUTLET -->` (`#float-wa-wrap` + `#float-wa-popup` 4 outlet + `#float-wa-btn` hijau, 33 baris) dihapus **berserta JS-nya** (toggle `floatBtn`/`floatPopup` + 1 baris di listener "tutup saat klik di luar" — wajib ikut dihapus, kalau tidak `addEventListener` pada null menghentikan sisa script inline). (2) `<body data-cart-fab="left">` → **`right`** (position FAB dihitung `assets/js/cart.js` baris 58 dari atribut ini) sehingga tombol "Keranjang" mengembang occupy posisi bekas WA. (3) Tombol `.cart-trigger` + `.cart-badge` di navbar (ikon keranjang, 4 baris) **dan** teks `.cart-trigger` "Keranjang" di menu mobile **keduanya dihapus** ("samakan dengan yang di desktop") → pemicu `.cart-trigger` di beranda = **0**, satu-satunya akses keranjang = FAB mengambang kanan bawah (desktop & HP sama). `produk.html` tidak diubah, navbar katalog masih ada. `index.html` 912 → **863 baris**; `float-wa` 0, `cart-trigger` 0, `cart-badge` 0 di HTML. Tailwind di-rebuild. **Pengujian**: `test-cart-shared.mjs` — **143 tes lulus** (tes FAB kiri & badge navbar diganti tes: FAB `right-5`, `float-wa` 0 di markup, pemicu 0, badge hanya 1 di FAB). **Belum dikerjakan**: commit + push (menunggu persetujuan pengguna).
- **2026-09-26** — **Tambah keterangan pembayaran di section Cara Pemesanan** (permintaan pengguna). **Versi final setelah koreksi pengguna**: TIDAK ada kartu/section baru — keterangan hanya masuk ke deskripsi **poin 3 "Konfirmasi & Bayar"**: "Setelah deal, lakukan pembayaran sesuai kesepakatan. Tidak wajib bayar di **muka** — bisa dibayar di tempat ketika barang sudah sampai di alamat Anda atau saat mengambil langsung di outlet." (Kartu oranye `bg-brand-50` + ikon uang yang sempat dibuat di bawah grid 4 langkah sudah **dihapus lagi** atas permintaan pengguna: "jangan dijadikan section baru".) `privacy.html` section 6 (Syarat & Ketentuan) tetap menambah bullet **Pembayaran** (tidak wajib bayar di muka / bayar di tempat saat barang tiba / harus dikonfirmasi outlet lewat WA) supaya konsisten dengan teks di halaman utama. Tailwind di-rebuild dua kali; `index.html` 912 baris (kembali seperti sebelum penambahan kartu); tes `test-cart-shared.mjs` **145 lulus**. **Belum dikerjakan**: commit + push (menunggu persetujuan pengguna).
